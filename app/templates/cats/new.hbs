{{new-cat-form 
  name=model.name 
  breed=model.breed 
  temperament=model.temperament
  weight=model.weight
  hobbies=newHobbies
  triggerSave=(action 'save')
  }}

  {{#if apiReturn}}
    {{message}}
  {{/if}}
